name: Dependabot

on:
  pull_request_target:
    types: [opened, synchronize, reopened]
  schedule:
    - cron: '0 */6 * * *'

jobs:
  dependabot:
    uses: France-Travail/pass-emploi-tools/.github/workflows/dependabot.yml@7fbddd28471c99d0a60b2d74db50c0c6f8c95215
