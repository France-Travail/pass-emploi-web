name: Security Alerts

on:
  schedule:
    - cron: '0 */3 * * *' # Toutes les 3 heures

jobs:
  security-alerts:
    uses: France-Travail/pass-emploi-tools/.github/workflows/security-alerts.yml@7fbddd28471c99d0a60b2d74db50c0c6f8c95215
    with:
      repo-name: 'pass-emploi-web'
    secrets:
      DEPENDABOT_ALERTS_READER: ${{ secrets.DEPENDABOT_ALERTS_READER }}
      MATTERMOST_WEBHOOK_URL: ${{ secrets.MATTERMOST_WEBHOOK_URL }}
