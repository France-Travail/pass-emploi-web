# Profil API STAGING : pointe vers pass-emploi-api de staging.
# Chargé AVANT .env.local (premier fichier gagne). Committé, sans secret.
NEXT_PUBLIC_API_ENDPOINT=https://api.pass-emploi.incubateur.net
KEYCLOAK_ISSUER=${KEYCLOAK_ISSUER_STAGING}